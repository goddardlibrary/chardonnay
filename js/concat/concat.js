'use strict';

/* jshint ignore:start */
(function ($) { Drupal.behaviors.rocketbooster = { attach: function (context, settings) {
/* jshint ignore:end */
// Start Drupal Behaviors

  console.log('jQuery ' + $().jquery + ' is firing.');

// End Drupal Behaviors
/* jshint ignore:start */
}};}(jQuery));
/* jshint ignore:end */